import {
    StyleSheet,
} from 'react-native';


export default StyleSheet.create({
    container: {
        display: 'flex',
        flexShrink: 1,
        flexGrow: 1,
        flexBasis: 0,
        height: '100%',
    }
});
